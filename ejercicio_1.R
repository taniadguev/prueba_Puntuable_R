
base <- as.numeric(readline("Ingresa el valor de la base del triángulo: "))
altura <- as.numeric(readline("Ingresa el valor de la altura del triángulo: "))


area <- (base * altura) / 2

cat("El área del triángulo es:", area, "\n")

